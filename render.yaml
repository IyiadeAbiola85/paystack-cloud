services:
  - type: web
    name: paystack-cloud
    env: node
    buildCommand: npm install
    startCommand: node server.js   # <-- must be this, not node src/server.js
    envVars:
      - key: PAYSTACK_SECRET_KEY
        sync: false
    plan: free